{
  "name": "miele-render",
  "private": true,
  "scripts": {
    "build": "echo \"No build needed\"",
    "start": "node shipping-policy-start.js",
    "build:cloudflare": "node cloudflare-build.js",
    "deploy:cloudflare": "npm run build:cloudflare && wrangler deploy"
  },
  "engines": {
    "node": ">=20.0.0 <22.0.0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.16.3",
    "stripe": "^14.0.0",
    "pdf-lib": "^1.17.1",
    "nodemailer": "^6.9.16",
    "libphonenumber-js": "^1.11.20"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
